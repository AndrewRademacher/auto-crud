var assert = require('assert');

describe('AutoCRUD', function() {
    describe('Arrays', function() {
        it('should be able to create values in an array belonging to an object', function() {
            assert(true);
        });

        it('should be able to update values in an array belonging to an object', function() {
            assert(true);
        });

        it('should be able to delete values in an array belonging to an object', function() {
            assert(true);
        });
    });
});
